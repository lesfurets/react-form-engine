import * as React from "react";
import {PredicateEditorComponentProps} from "../PredicateEditor";

export const ValuePredicateEditor: React.FunctionComponent<PredicateEditorComponentProps> = ({predicate, onChange}) => {
    return (
        <div>ValuePredicateEditor</div>
    );
};