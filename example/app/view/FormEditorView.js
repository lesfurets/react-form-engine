import React from "react";

import "../../../src/styles/components/view/form-view.less"

export const FormEditorView = (props) => (
    <div className="FormView">
        {props.children}
    </div>
);